# Hermes Agent Environment Configuration
# 复制为 .env 后填入 API keys

# API keys（根据你的 provider 填写）
# OPENROUTER_API_KEY=
# FIREWORKS_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# 自定义 provider（配置在 config.yaml custom_providers 中）
# 如果使用了 custom_providers，api_key 配置在 config.yaml 中
